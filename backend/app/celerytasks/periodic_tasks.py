"""
here should the periodic tasks lie, once celery beat is implemented

currently planned periodic tasks:
- syncAppCatalogue
- delete_orphaned_instances (if we decide to implement this)
- delete_expired_demo_instances (once a demo instances ttl is 0, it should be deleted)

"""